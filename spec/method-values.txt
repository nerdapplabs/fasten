# fasten canonical method values
#
# The HOW anchor — how the audit event was triggered.
# Wire format: lowercase string in the `method` field of every audit row.
# Use "sdk" when no transport shim is active (direct programmatic call).

http
mqtt
cli
scheduler
ui
agent_tool
sdk
